SELECT id, valor
		FROM (
			SELECT tin.id, tin.idPosicion, tin.valor
			FROM incognitas tin
			INNER JOIN partidas tp ON tp.id = tin.idPartida
			INNER JOIN posiciones tpos ON tpos.id = tin.idPosicion
			WHERE tpos.fila = 1 AND tp.id = 1 AND tin.valor IS NOT NULL
		)	
/
